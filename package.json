{
  "name": "rns",
  "private": true,
  "scripts": {
    "web:start": "cd packages/webapp && yarn start",
    "server:start": "cd packages/server && yarn start:dev",
    "dto:compile": "cd packages/dtos && yarn compile"
  },
  "devDependencies": {
    "lerna": "^5.1.4"
  },
  "dependencies": {
    "@prisma/client": "^4.2.1"
  }
}
